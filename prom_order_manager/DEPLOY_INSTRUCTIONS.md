# 🚀 Инструкция по деплою на Render.com

Этот бот подготовлен для работы на Render в режиме **Web Service** (бесплатный или платный тариф).

## Шаг 1: Подготовка Git
1. Загрузите всю папку `prom` (или только `prom_order_manager`) в ваш репозиторий на **GitHub** или **GitLab**.
   - Рекомендуется сделать репозиторий **Private** (частным), так как в коде могут быть чувствительные данные (хотя мы используем переменные окружения, лучше перестраховаться).

## Шаг 2: Создание сервиса на Render
1. Зарегистрируйтесь/войдите на [dashboard.render.com](https://dashboard.render.com/).
2. Нажмите **New +** -> **Web Service**.
3. Подключите ваш GitHub/GitLab репозиторий.
4. Выберите репозиторий с ботом.

## Шаг 3: Настройка
Заполните форму следующими данными:

| Поле | Значение | Примечание |
|------|----------|------------|
| **Name** | `prom-order-bot` (или любое) | |
| **Region** | `Frankfurt` (ближе к Украине) | |
| **Branch** | `main` (или master) | |
| **Root Directory** | `prom_order_manager` | **ВАЖНО!** Укажите имя папки, где лежит `main.py` |
| **Runtime** | `Python 3` | |
| **Build Command** | `pip install -r requirements.txt` | |
| **Start Command** | `python main.py` | |

## Шаг 4: Переменные окружения (Environment Variables)
Прокрутите вниз до раздела **Environment Variables** и добавьте ключи из вашего `.env` файла:

1. `PROM_API_TOKEN` = `ваш_токен_пром`
   - Если магазинов несколько, перечислите токены через запятую: `токен1,токен2`
2. `TELEGRAM_BOT_TOKEN` = `ваш_токен_бота`
3. `TELEGRAM_CHAT_ID` = `ваш_чат_id`
4. `SHARED_DATA_PATH` = `prom_import_data.json` (Опционально, бот создаст его сам)
5. `PYTHON_VERSION` = `3.10.0` (Рекомендуется)

## Шаг 5: Запуск
Нажмите **Create Web Service**.
Render начнет сборку. Если все ок, через 1-2 минуты бот запустится и статус станет **Live**.

## Как это работает?
* Бот запускает маленький веб-сервер, чтобы Render "видел", что приложение живо.
* Одновременно с этим бот проверяет заказы и слушает Телеграм на предмет новых файлов `prom_import_data.json`.
* **Бесплатный тариф:** На бесплатном тарифе Render "засыпает" через 15 минут бездействия. Чтобы бот работал 24/7, используйте платный тариф ($7/мес) или настройте внешний пинг (например, через UptimeRobot) на URL вашего сервиса (будет выдан Рендером, вида `https://prom-bot.onrender.com`).
